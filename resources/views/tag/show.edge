@layout('layouts.main')

@section('content')
  @set('pageTitle', tag.title)
  <div class="container">
  <h1 class="mt-4 mb-4 font-weight-light">{{ tag.title}}</h1>
  @!component('components.post.list', {
    items:data
  })
  @!component('components.pagination', {
    total,
    perPage,
    page,
    lastPage
  })
  </div>
  
@endsection